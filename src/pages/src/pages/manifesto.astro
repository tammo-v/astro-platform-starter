---
import Layout from '../layouts/Layout.astro'
import manifesto from '../content/manifesto.md' // we'll move it into a `content/` folder

const { Content } = await manifesto()
---

<Layout title="Manifesto">
  <Content />
</Layout>
