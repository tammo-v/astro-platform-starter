---
import Layout from '../layouts/Layout.astro'
import { getEntryBySlug } from 'astro:content'
import { MarkdownContent } from 'astro:content'

const entry = await getEntryBySlug('manifesto', 'manifesto')
---

<Layout title="Manifesto">
  <MarkdownContent content={entry} />
</Layout>
