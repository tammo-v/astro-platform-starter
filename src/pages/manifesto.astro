---
import Layout from '../layouts/Layout.astro'
import { getEntryBySlug } from 'astro:content'
import Content from '@astro/components/Content.astro'

const entry = await getEntryBySlug('manifesto', 'manifesto')
---

<Layout title="Manifesto">
  <Content entry={entry} />
</Layout>
